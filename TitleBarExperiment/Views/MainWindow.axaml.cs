using Avalonia.Controls;

namespace TitleBarExperiment.Views;

public partial class MainWindow : Window {
    public MainWindow() {
        InitializeComponent();
        new Window0().Show();
    }
}
